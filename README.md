# test

## 実行手順

1. Excel などの VBA エディターを開き、プロジェクトに `vba/modTetris.bas` を追加します。
2. `SetupTetris` マクロを実行して初期化します。
3. テトリスを開始するには `StartTetris` マクロを実行します。
4. テトリスを終了するには `StopTetris` マクロを実行します。
5. 実装は最小限のため、デバッグウィンドウでメッセージを確認してください。
